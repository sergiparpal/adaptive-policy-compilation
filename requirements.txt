# Dependencies of the real proposer. Steps 0 and 1 and all of rungs 2, 3 and 4
# run on the standard library: this is only needed in order to spend money.
#
# ENVIRONMENT EVERYTHING RECORDED WAS PRODUCED WITH
#   Python 3.12.3 (GCC 13.3.0) on Linux x86_64
#   openai 2.53.0
#
# The version is PINNED, not bounded. It used to say `openai>=1.40.0`, which in
# practice meant "whichever was around that day": rebuilding the environment of
# a recorded run was impossible. The full transitive closure, as it was
# installed, is in requirements.lock.txt.
#
# OpenRouter is compatible with the OpenAI API, so this same package serves both
# providers.
openai==2.53.0

# Only if you use --provider anthropic. Unpinned on purpose: it has not been
# used in any recorded run, so there is no version to reproduce. If it ever is
# used, pin it here before running anything.
# anthropic>=0.40.0
