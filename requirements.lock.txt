# Cierre transitivo de openai==2.53.0, tal como estaba instalado en el entorno
# que produjo los registros de results/ y results2/. Generado con
# `pip freeze` el 7 de agosto de 2026 y podado de dos paquetes que estaban en
# el venv pero NO son dependencias del proyecto (git-filter-repo, instalado
# aparte como herramienta).
#
#   Python 3.12.3 · Linux x86_64
#
# Para reconstruir exactamente ese entorno:
#   python3 -m venv .venv
#   .venv/bin/pip install -r requirements.lock.txt
#
# Para instalar solo lo declarado y dejar que pip resuelva el resto, usa
# requirements.txt.
annotated-types==0.8.0
anyio==4.14.2
certifi==2026.7.22
distro==1.9.0
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.18
jiter==0.16.0
openai==2.53.0
pydantic==2.13.4
pydantic_core==2.46.4
sniffio==1.3.1
tqdm==4.70.0
typing-inspection==0.4.2
typing_extensions==4.16.0
