# Transitive closure of openai==2.53.0, as it was installed in the environment
# that produced the records in results/ and results2/. Generated with
# `pip freeze` on August 7, 2026 and pruned of two packages that were in the
# venv but are NOT project dependencies (git-filter-repo, installed separately
# as a tool).
#
#   Python 3.12.3 · Linux x86_64
#
# To rebuild exactly that environment:
#   python3 -m venv .venv
#   .venv/bin/pip install -r requirements.lock.txt
#
# To install only what is declared and let pip resolve the rest, use
# requirements.txt.
annotated-types==0.8.0
anyio==4.14.2
certifi==2026.7.22
distro==1.9.0
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.18
jiter==0.16.0
openai==2.53.0
pydantic==2.13.4
pydantic_core==2.46.4
sniffio==1.3.1
tqdm==4.70.0
typing-inspection==0.4.2
typing_extensions==4.16.0
